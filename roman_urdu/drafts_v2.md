# Roman Urdu drafts from the fine-tuned model

Read these and answer one question: **would correcting this be faster than
writing it from scratch?**

If yes, the domain corpus is worth building and these are its first rows.
If no, stop - the same answer opus-mt gave, and the same conclusion.

**50 lines · placeholders kept 41/43 (95%)** (opus-mt managed 0/43)

---

### 1. `purpose`

**EN** To merge two sorted arrays into one sorted array in-place.

**RU** Do sorted arrays ko aik sorted array mein merge kar rahe hain.

### 2. `algorithm`

**EN** The function recursively calculates the height of each subtree (left and right) and returns the maximum of these heights plus one (for the current node).

**RU** The function recursively calculates the height of each⟦0⟧ aur returns the maximum of these heights plus⟦1⟧.

### 3. `purpose`

**EN** To construct an object of type T at a specified memory location `p` with the given value `v`.

**RU** Type T ke liye aik object ko ek specified memory location mein⟦0⟧⟦1⟧ ke saath construct kar raha hai.

### 4. `algorithm`

**EN** Computes the determinant using the formula ad - bc.

**RU** determinant ko formula ad - bc istemal karta hai.

### 5. `output`

**EN** An integer representing the minimum number of deletions required.

**RU** Aik integer jo ke mukhtalif deletions ka samna karta hai.

### 6. `input`

**EN** A string `title` and an `Array` object `tab`.

**RU** Aik string⟦0⟧ aur⟦1⟧ object⟦2⟧.

### 7. `purpose`

**EN** To remove any leading hyphens or slashes from a given string.

**RU** To remove any leading hyphens ya slashes from a given string.

### 8. `algorithm`

**EN** Sets the node's data, left child, and right child to the provided values and updates the internal state.

**RU** Node ki data set karta hai, left child, aur right child provided values par set karta hai aur internal state update karta hai.

### 9. `algorithm`

**EN** The function uses a static array of strings to map the status enumeration values to their corresponding messages. It checks if the provided status is within the valid range and returns the appropriate message. If the status is out of range, it returns "Unknown status".

**RU** The function uses a static array of strings to map the status enumeration values to their corresponding messages. It checks if the provided status is within the valid range aur returns the appropriate message. If the status is out of range, it returns "Unknown status".

### 10. `output`

**EN** An integer representing the result of the RPN expression.

**RU** Aik integer RPN expression ke result ko represent karta hai.

### 11. `algorithm`

**EN** Uses Boost.Algorithm's `find_format_all` function with `first_finder` and `const_formatter` to locate and replace all instances of the search sequence with the format sequence.

**RU** ⟦0⟧ ka⟦1⟧ function ko⟦2⟧ aur⟦3⟧ ke saath istemal kiya jata hai taake search sequence ke saath format sequence ke saath saath locate aur replace karne ke liye.

### 12. `output`

**EN** The new length of the vector after removing duplicates.

**RU** Vector ka naye length ko duplicates ko remove kar raha hai.

### 13. `algorithm`

**EN** Iterates through the substring from both ends towards the center, comparing characters at corresponding positions. If any pair of characters does not match, returns false; otherwise, returns true after completing the iteration.

**RU** Do ends se center ke saath substring mein iterates karta hai, jo characters ko corresponding positions mein compare karta hai. If kuch pair of characters na match karta hai, false; otherwise, true after completing the iteration.

### 14. `input`

**EN** Two `cancellation_type_t` parameters `x` and `y`.

**RU** Do⟦0⟧ parameters⟦1⟧ aur⟦2⟧.

### 15. `output`

**EN** void - No value is returned; the function simply causes the current thread to pause.

**RU** void - No value returned karta hai; the function simply causes the current thread to pause.

### 16. `purpose`

**EN** To eliminate consecutive duplicate characters from a string.

**RU** String se consecutive duplicate characters ko behtar banane ke liye.

### 17. `algorithm`

**EN** The function first computes the squared distance from the origin. If this distance is less than half the square of the circle's radius, it calculates the perpendicular distance from the origin to the line segment connecting the origin to the point. Otherwise, it returns the maximum possible distance (half the circle's diameter).

**RU** The function first computes the squared distance from the origin. If this distance is less than half the square of the circle's radius, it calculates the perpendicular distance from the origin to the line segment connecting the origin to the point. Otherwise, it returns the maximum possible⟦0⟧.

### 18. `algorithm`

**EN** The function sums the corresponding coordinates of the three points and then divides each sum by 3 to find the centroid's coordinates.

**RU** The function sums the corresponding coordinates of the three points aur then divides each sum by 3 to find the centroid's coordinates.

### 19. `algorithm`

**EN** Use two pointers to track the slow (`s`) and fast (`f`) positions. If the current element is not equal to the element two positions before it, copy it to the slow pointer position and move both pointers forward. Otherwise, only move the fast pointer forward.

**RU** ⟦0⟧ aur⟦1⟧ positions ko track karne ke liye do pointers ka istemal karne ke liye. If the current element is not equal to the element two positions before it, copy it to the slow pointer position aur move both pointers forward. Otherwise, only move the fast pointer forward.

### 20. `purpose`

**EN** This function computes the intersection of two meshes A and B, storing the result in a given mesh object. It uses a boolean operation with the operator "A*B".

**RU** Yeh function do meshes A aur B ke intersection ko compute karta hai, result ko ek given mesh object mein storage karta hai. Yeh operator A*B ke saath boolean operation istemal kiya jata hai.

### 21. `purpose`

**EN** Calculate the binomial coefficient (n choose r).

**RU** binomial⟦0⟧ ko calcul kar raha hai.

### 22. `algorithm`

**EN** Iterates through both lists, comparing nodes and appending the smaller node to the result list until one of the lists is exhausted. Then appends the remaining nodes from the other list.

**RU** Do lists ke zariye iterates karta hai, nodes ko compare karta hai aur chhote node ko result list ko appending karta hai jab ek list ko exhausted karta hai. Aap append karta hai the remaining nodes from the other list.

### 23. `purpose`

**EN** Finds the longest palindromic substring in a given string.

**RU** Aik bhi sab se sab se sab se palindromic substring ko khel raha hai.

### 24. `algorithm`

**EN** Recursively reverse the sublist by calling `reverse()` on the next node until it reaches the `tail`. Then, adjust the pointers to reverse the current node's link and finally return the new head of the reversed sublist.

**RU** Recursively reverse the sublist by calling⟦0⟧ on the next node until it reached the⟦1⟧. Then adjust the pointers to reverse the current node's link aur finally return the new head of the reversed sublist.

### 25. `algorithm`

**EN** Iterate through the array. Maintain a counter that increments for each zero encountered. When a '1' is found, check if the counter is less than `k`. If so, return false. Reset the counter to 0 if it meets or exceeds `k`.

**RU** Is array ke zariye aik counter maintain karta hai jo har zero ke liye increments karta hai. When a '1' is found, check karta hai if the counter is less than⟦1⟧. If so, return false. Reset the counter to 0 if it meets ya exceeds⟦0⟧.

### 26. `algorithm`

**EN** Converts the string 'b' into its integer equivalent modulo 'a' by iterating through each character, converting it to an integer, and updating the modulo value.

**RU** String 'b' ko apne integer equivalent modulo 'a' mein convert kar raha hai har character ke through iterating, converting it to an integer, aur updating the modulo value.

### 27. `algorithm`

**EN** Uses Kadane's algorithm to find the maximum subarray sum efficiently. It iterates through the array while maintaining a current sum (`curr_sum`) and updating the maximum sum (`max_sum`). If the current sum becomes negative, it resets to zero since a negative sum would decrease the sum of any subsequent subarray.

**RU** Kadane ke algorithm ka istemal karne ke liye maximum subarray sum ko tayyar karne ke liye tayyar kiya jata hai. Is array mein iterates karta hai while maintaining a current⟦0⟧ aur updating the maximum⟦1⟧. If the current sum becomes negative, it resets to zero since a negative sum would decrease the sum of any subsequent subarray ko ko Kadane ke liye.

### 28. `input`

**EN** `std::ostream& o` - the output stream; `const Expr& e` - the expression to be formatted.

**RU** ⟦0⟧ - output stream;⟦1⟧ - formatted expression.

### 29. `purpose`

**EN** Deletes a node with a given key from a BST and returns the updated root.

**RU** BST ke saath aik node ko delete karta hai aur updated root returns karta hai.

### 30. `algorithm`

**EN** Uses two passes to partition the array. The first pass moves elements less than `cutval` to the front, and the second pass moves elements equal to `cutval` to the end. Limits are set after both passes complete.

**RU** Do passes ko partition karne ke liye istemal kiya jata hai. The first pass elements ko⟦0⟧ ko front karta hai, aur the second pass elements ko⟦1⟧ ko end karta hai. Limits set karta hai after both passes complete hain.

### 31. `algorithm`

**EN** The function simply assigns the pid and tid values from the 'from' object to the 'to' object.

**RU** The function simply assigns the pid aur tid values from the 'from' object to the 'to' object.

### 32. `algorithm`

**EN** Use dynamic programming to store the minimum cost to reach each step in a vector `dp`. For each step, calculate the minimum cost to reach it from the previous two steps and add the current step's cost. Return the minimum cost to reach the last step.

**RU** Dynamic programming istemal karne ke liye minimum cost ko store karne ke liye vector⟦0⟧ mein har step ke liye. For each step, calculate the minimum cost to reach it from the previous two steps aur add the current step's cost. Return the minimum cost to reach the last step.

### 33. `purpose`

**EN** To compute a triangular window value for an input within the range [-1, 1].

**RU** ⟦0⟧ mein input ke liye triangular window value compute kar raha hai.

### 34. `purpose`

**EN** To create a copy of a sequence with trailing characters removed according to a given predicate.

**RU** To create a copy of a sequence with trailing characters removed according to a given predicate.

### 35. `algorithm`

**EN** The function sorts the folder paths lexicographically and iterates through them. It adds each folder to the result if it is not a subfolder of the previously added folder.

**RU** The function sorts the folder paths lexicographically aur iterates through them. It adds each folder to the result if it is not a subfolder of the previously added folder.

### 36. `algorithm`

**EN** Iterates through the benchmark data to calculate and print various statistics such as number of vertices/edges/faces, construction/query times, and peak memory usage.

**RU** Benchmark data ke zariye istemaal karta hai takay mukhtalif statistics ko calcul aur print karne ke liye, jaise ke number of vertices/edges/faces, construction/query times, aur peak memory usage.

### 37. `algorithm`

**EN** Adjusts the prev and next pointers of adjacent nodes to bypass the target node. Updates head and tail pointers of the group if necessary. Finally, sets the removed node's pointers to null.

**RU** Aaj ke naslon ke prev aur next pointers ko adjust karne ke liye target node ko bypass karne ke liye. Group ke head aur tail pointers ko update karta hai if necessary. Final, removed node ke pointers null ko set karta hai.

### 38. `input`

**EN** A vector of strings where each string is either an operator ('+', '-', '*', '/') or an integer.

**RU** Strings ka vector hai jahan har string ek⟦0⟧ ya integer hai.

### 39. `output`

**EN** A vector of strings representing the summary ranges.

**RU** Strings ka vector jo summary ranges ko represent karta hai.

### 40. `algorithm`

**EN** Iterates through the wide character sequence, appending each character to a temporary `std::string`. Then, it uses Boost.Regex's `lookup_default_collate_name` to get the default collate name for the constructed string. If the result is empty, it checks if the sequence contains only one character and returns it as a `std::wstring`. Otherwise, it returns an empty string.

**RU** Iterates through the wide character sequence, appending each character to a temporary⟦0⟧. Then, it uses⟦1⟧'s⟦2⟧ to get the default collate name for the constructed string. If the result is empty, it checks if the sequence contains only one character aur returns it as a empty string. Otherwise, it returns an empty string.

### 41. `algorithm`

**EN** The function uses dynamic programming to solve this problem. It maintains a DP table `dp` where `dp[i][0]` represents the length of the longest turbulent subarray ending at index `i` that ends with a decrease from its previous element, and `dp[i][1]` represents the length of the longest turbulent subarray ending at index `i` that ends with an increase from its previous element. The function iterates through the array, updating the DP table based on whether the current element is greater than, less than, or equal to the previous element. The result is the maximum value in the DP table.

**RU** The function uses dynamic programming to solve this problem. It maintains a DP table where⟦1⟧ represents the length of the longest turbulent subarray ending at index⟦0⟧ that ends with a decrease from its previous element, aur⟦2⟧ represents the length of the longest turbulent subarray ending at index⟦1⟧ that ends with an increase from its previous element. The function iterates through the array, updating the DP table based on whether the current element is greater than, less than, ya equal to the previous element. The result is the

### 42. `output`

**EN** `int` - Returns 0 on success, or -1 if there was an error during the parsing process.

**RU** ⟦0⟧ - Erfolg ko 0 return karta hai, ya -1 if error ke sath parsing process ke sath.

### 43. `input`

**EN** A 2D vector of characters (`vector<vector<char>>`), a starting row index (`int`), a starting column index (`int`), and a target word (`string`).

**RU** ⟦0⟧ ka 2D vector, aik starting row⟦2⟧, aik starting column⟦1⟧, aur aik target⟦2⟧.

### 44. `output`

**EN** The function returns void but modifies the cell's info field directly.

**RU** The function returns void but modifies the cell's info field directly.

### 45. `algorithm`

**EN** The function uses logarithms to calculate the quotient. If either input is zero, it returns appropriate values. It also checks for overflow and adjusts the sign based on the inputs.

**RU** The function uses logarithms to calculate the quotient. If either input is zero, it returns appropriate values. Is also checks for overflow aur adjusts the sign based on the inputs.

### 46. `algorithm`

**EN** Iterates through the source list and transfers elements to the destination list if they are not already present in the destination list as determined by the comparator.

**RU** Source list ke zariye iterates karta hai aur elements ko destination list mein transfer karta hai if they are not already present in the destination list as determined by the comparator.

### 47. `output`

**EN** None (`void`). The function prints the board to the standard output.

**RU** ⟦0⟧. The function print the board to the standard output.

### 48. `purpose`

**EN** This function combines parameters for block merging in a sorting algorithm, calculating necessary block sizes and optionally initializing keys.

**RU** Ye function block merging ke liye parameters ko sorting algorithm mein shamil karta hai, necessary block sizes ko calculate karta hai aur optionally initializing keys.

### 49. `algorithm`

**EN** Copies members from the existing arc to a new representation and updates the minimum and maximum points before creating and returning a new Rebound_arc_2 object.

**RU** Kopies members existing arc se aik new representation aur updates the minimum aur maximum points before creating aur returning a new⟦0⟧ object.

### 50. `purpose`

**EN** Calculate the number of days from the year 0 to a given year, considering leap years.

**RU** Aik saal 0 se aik saal ke saal ke saal ka saal karta hai, leap saalon ko samajhne ke liye tayyar karta hai.
